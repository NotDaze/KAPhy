if(!Draw) {
  var CENTER = "CENTER";
  var CORNER = "CORNER";
  var RADIUS = "RADIUS";
  var CORNERS = "CORNERS";
  
  var Draw = {
    currentRectMode: "CORNER",
    currentEllipseMode: "CENTER",
  };
}
